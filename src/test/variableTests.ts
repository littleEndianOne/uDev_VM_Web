import { expect } from "chai";

describe('Tests Description', function () {
  describe('Sub Tests Description', function () {

    it('should test something...', function () {
      
      expect(1).to.equal(1);
      

     // expect(compiler.errors).to.have.length(0, "An unexpected error was generated");
     // expect(compiler.symbolTable.symbolCount).to.be.greaterThan(0, "Symbol not added to symbol table");     
    //  expect(symbol.symbolType, "Symbol not of type Variable as expexted").equals(SymbolType.variable);
     // expect(varSymbol.varType, "Symbol is not of type integer as expected").to.equal(VarTypes.integer);
     // expect(varSymbol.assignedTo, "The variable has not been assigned to as expected").to.be.false;
    });
  });
});


